@page
@model ControlAVP.Pages.Devices.SonyKDL60W855Model

@{
    ViewData["Title"] = "Sony KDL60W855";
}
<h2></h2>


<form method="post">
@*@if (false)
{
    <div class="alert alert-warning">
        <strong>Warning!</strong> Device not found, is it turned on?
    </div>
}
else
{*@
    <div class="card">
        <div class="card-header">
            <h6 class="mb-0">@ViewData["Title"]</h6>
        </div>
        <div class="card-body">
            <div class="btn-group">
                <button type="submit" asp-page-handler="TurnOn" class="btn btn-success border">On</button>
                <button type="submit" asp-page-handler="TurnOff" class="btn btn-danger border">Off</button>
            </div>
        </div>
    </div>
@*}*@
</form>